services:
  - type: web
    name: maternite-manager
    env: node
    rootDir: .
    buildCommand: npm install && npm run build
    startCommand: node server/src/index.js
    plan: free
    autoDeploy: true
